{% extends 'base.html.twig' %}

{% block title %}Tache{% endblock %}

{% block body %}
    <h1>Tache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tache.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tache.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tache.description }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ tache.Statut }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ tache.photo }}</td>
            </tr>
            <tr>
                <th>Date_de_fin</th>
                <td>{{ tache.dateDeFin ? tache.dateDeFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div class="btn btn-primary mt-4">
    <a href="{{ path('tache') }}"style="color:white;text-decoration:none">Retourner à la liste</a>
    </div>
<div class="btn btn-primary mt-4">
    <a href="{{ path('app_tache_edit', {'id': tache.id}) }}"style="color:white;text-decoration:none">Modifier</a>
</div>
    {{ include('tache/_delete_form.html.twig') }}
{% endblock %}
